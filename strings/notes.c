// Samantha Naranjo, strings c
#include <stdio.h>
#include <string.h>
char subject[50];
char name[] = "Victoria"

int main(void){
    //printf("What class are you in?\n");
    //scanf("%s", subject);
    //fgets(subject, 50, stdin);
    //printf("you are in %s, that is a cool class", subject);
    //name[0] = 'T';
    //name[1] = 'o';
    //name[2] = 'r';
    //name[3] = 'i';
    //print("%s", name);
    //printf("%lu",sizeof(sentence))
    //printf("%d",strlen(sentence))
    str one[] = "hello";
    char two[] = "world!";
    strcat(one, two);
    return 0;
    }